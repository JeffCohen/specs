<div class="page-header">
  <h1>Projects</h1>
</div>

<p><%= link_to 'Add a New Project', new_project_url, class: 'btn btn-primary' %></p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @projects.each do |project| %>
    <tr>
      <td><%= link_to project.title, project_url(project), class: 'btn' %></td>
    </tr>
  <% end %>
  </body>
</table>

